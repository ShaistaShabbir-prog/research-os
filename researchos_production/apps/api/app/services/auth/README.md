# services/auth/

This directory is a placeholder stub from the original project scaffold.

Authentication is implemented in `../auth_service.py` (JWT, PBKDF2 passwords, register/login/verify).

This directory can be removed in a future cleanup PR.
