# services/llm/

Placeholder for future LLM provider abstraction layer.
Current Claude integration is direct via `httpx` in `ai_review_copilot.py`.

Future: support OpenAI, Gemini, and Anthropic via unified interface.
